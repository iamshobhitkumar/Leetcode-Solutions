# Write your MySQL query statement below
select project_id, round(avg(e1.experience_years),2) as average_years from Project p1, Employee e1 where p1.employee_id = e1.employee_id group by p1.project_id;